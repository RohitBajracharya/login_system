import 'package:flutter/material.dart';

class DashboardOfFragmetns extends StatelessWidget {
  const DashboardOfFragmetns({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Dashboard"),
      ),
    );
  }
}
